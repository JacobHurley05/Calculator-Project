import tkinter as tk

window = tk.Tk() #create a window
window.title("My Calculator") #title of the window
window.geometry("400x600") #size of the window



#execute the window
window.mainloop() #show the window